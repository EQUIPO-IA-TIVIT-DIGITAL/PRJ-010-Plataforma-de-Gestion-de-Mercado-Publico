export interface ${ENTITY}ListItem {
  id: number;
  createdDate: string;
}

export interface ${ENTITY}DetailResponse {
  id: number;
  createdDate: string;
}

export interface Create${ENTITY}Request {
  [key: string]: unknown;
}

export interface Create${ENTITY}Response {
  id: number;
}

export interface Update${ENTITY}Request {
  [key: string]: unknown;
}

export interface Update${ENTITY}Response {
  id: number;
}

export interface ${ENTITY}QueryParams {
  page?: number;
  pageSize?: number;
  sortBy?: string;
  sortOrder?: 'ASC' | 'DESC';
  searchFilter?: string;
}

export interface ${ENTITY}ListResponse {
  items: ${ENTITY}ListItem[];
  total: number;
  page: number;
  pageSize: number;
}
