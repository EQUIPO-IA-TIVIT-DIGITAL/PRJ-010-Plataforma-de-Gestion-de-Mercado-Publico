/* ============================================================
   Table: $SCHEMA.$ENTITY
   Description: Stores $entities
   Author: Framework Generated
   ============================================================ */
CREATE TABLE $SCHEMA.$ENTITY (
    ${ENTITY}Id INT IDENTITY(1,1) NOT NULL,
    CreatedBy INT NOT NULL,
    CreatedDate DATETIME2 NOT NULL DEFAULT GETUTCDATE(),
    UpdatedBy INT NULL,
    UpdatedDate DATETIME2 NULL,
    RecordStatus CHAR(1) NOT NULL DEFAULT 'A',
    CONSTRAINT PK_${ENTITY} PRIMARY KEY (${ENTITY}Id)
);

-- Index for active records
CREATE NONCLUSTERED INDEX IX_${ENTITY}_RecordStatus 
    ON $SCHEMA.$ENTITY (RecordStatus)
    INCLUDE (${ENTITY}Id, CreatedDate);
